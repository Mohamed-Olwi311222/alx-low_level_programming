#include "main.h"
#include <string.h>
/**
 * _puts - prints a string from main
 * @str: recive it from main
 * Return: no return value (void)
 */
void _puts(char *str)
{
	char str1 = *str;

	puts(str1);
}
