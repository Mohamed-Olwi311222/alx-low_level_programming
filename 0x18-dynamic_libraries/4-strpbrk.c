#include "main.h"
/**
 * _strpbrk - finds the 1st char in accept that matches any char in s
 * @s: the string to search into
 * @accept: the char to search for
 * Return: it returns a pointer to char in s that matches
 */
char *_strpbrk(char *s, char *accept)
{
}
