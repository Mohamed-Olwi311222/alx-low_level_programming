#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: take it from main
 * Return: the length of the string in int
 */
int _strlen(char *s)
{
	int length;
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	length = i;
	return (length);
}
